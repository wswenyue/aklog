---
description: aklog 项目代码标准规范与代码修改交付规范
alwaysApply: true
---

# 项目代码标准规范

## 通用原则

- 遵循周边代码风格，优先小而聚焦的 diff
- 不做无关重构、顺手修复或范围蔓延
- 注释仅用于解释非显而易见的业务逻辑
- 避免过度抽象；复用 `aklog.core`、`aklog.log`、`aklog.cli` 中的现有工具

## Python 约定

- 行宽 120；使用 `ruff` 做 lint 与格式化
- 测试放在 `tests/`，目录结构与 `src/aklog/` 对应
- 本地运行模块时使用 `python3`，并设置 `PYTHONPATH=src`
- 命名、结构与 import 风格与周边代码保持一致

# 代码修改交付规范

凡涉及代码改动的任务，**告知用户修改完成、可交付之前，必须先完整执行 [code-quality skill](.cursor/skills/code-quality/SKILL.md) 并全部通过**。未执行或未通过 skill 验证，不得宣称本次修改已完成。

## 交付条件

1. **必须执行 skill 验证** — 按 skill 完成范围检查、自动化检查、人工审查、修复循环
2. **验证通过后方可交付** — 只有 skill 全部通过后，本次修改才算完成
3. **汇报须含验证结果** — 最终回复中说明已执行的检查及结果（见 skill 第 9 步）

不得自行简化或跳过 skill 中的任何步骤。

**例外：** 用户仅要求纯分析、解释或审查，且无代码改动时，可跳过 skill 验证。
